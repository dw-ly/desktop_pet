---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 共同养成
  - 纪念日
created: 2026-08-01
---

# 04 共同养成与纪念日实现

> 关系的"共同资产"：一起养团子（亲密度成长）与纪念日仪式。这两个功能制造**长期使用理由**，是对抗"新鲜感流失"的关键设计。

---

## 一、共同养成（pet.feed）

### 产品定义

```text
两个人共同养一只团子：
  - 互动越多 → 团子经验/亲密度越高 → 解锁更多装扮与动作
  - 团子同时存在于两端，状态保持一致
  - 纪念日解锁情侣专属装扮

价值：给关系一个"共同的进度条"，制造持续互动的理由
```

### 数据模型

```python
# core/pet_growth.py
@dataclass
class PetState:
    level: int = 1          # 等级 = f(exp)
    exp: int = 0            # 经验（等级成长）
    intimacy: int = 0       # 亲密度（关系进度）
    streak_days: int = 0    # 连续共同登录天数
    streak_status: str = "normal"   # normal 正常 | grace 灰色期
    grace_left: int = 3     # 灰色期剩余容错天数（漏登 -1，耗尽清零）
    grace_progress: int = 0 # 灰色期连续恢复日（达 3 恢复 normal）
    unlock: list[str]       # 已解锁装扮/动作 id
    custom_egg: dict        # 双方写入的彩蛋（纪念日解锁）
```

### 亲密度积分规则

> 本表为唯一来源，spec 以 [[7-AI学习/11-AI桌宠情侣互联/spec/pet-growth/spec.md\|pet-growth spec]] 积分来源表为准。

| 行为 | 基础积分 | 触发 | 防滥用 |
|------|:----:|------|--------|
| 带话送达 | +10 | msg.carry 成功送达 | 双方各计一次；每带话限 1 次/5min |
| 共同对话（当日首次） | +5 | 双方 24h 内都有对话 | 每日上限 1 次 |
| 日常喂食/摸头 | +3 | 本地交互 | 每日上限 5 次 |
| 礼物接受 | +5（普通）/ +20（纪念日特惠） | gift.accept 到达 | 双方各计一次；以接受事件为准 |
| 连续共同登录 | +streak×2 | 双方均在线 | 以自然日计；中断进入灰色期 |

> 纪念日加成（统一规则）：当天基础积分 ×2（礼物接受按 +20 特惠，不叠加 ×2），每年同日限一次。

> 共同登录灰色期（连续登录容错）：某日任一方无活动不立即清零，转"灰色期"（灰色计数 3 天）。灰色期共同登录日照常累计（streak+1、×2 积分），连续 3 个共同登录日恢复 normal；漏登灰色计数 -1 且恢复日清零，耗尽（0）则 streak 清零重来。灰色期界面显示灰色标签，数值照常。

### 增量同步与防作弊

```text
原则：本地计算增量 → 签名 → 同步 → 两端合并
  每次事件附：
    {delta: +10, reason: "carry", ts, sig: HMAC(intimacy_key, data)}

  签名密钥：由会话密钥派生（仅双方知道）
  合并规则（见 06-数据一致性）：
    - 以事件 ts 排序
    - 相同 reason 每日上限内只计一次
    - 校验 sig，非法则丢弃并告警
```

### 成长系统

```text
等级 = int(sqrt(exp / 100))          # 前期快、后期慢（经验 = 100·等级²）
等级解锁里程碑（spec 决议：5/10/15 级，配置表驱动，M3 内测后调优）：
  L5  解锁 1 套普通装扮
  L10 解锁 1 个专属动作/装扮
  L15 解锁 1 个高级装扮/徽章
解锁物品池（早期设计参考，具体分配 M3 内测后调整）：
  摇尾巴动作、普通装扮、陪伴动画（互相贴贴）、自定义彩蛋位

亲密度里程碑 → 情侣专属：
  100   解锁"爱心气泡"
  365   解锁"纪念日套装"
  1000  解锁"羁绊徽章"（桌面显示连心标记）

连续共同登录 → streak 徽章（每日递增；中断后进入 3 天灰色期，可恢复）
```

### 定时校验

```text
每日 0 点：两端各自结算 streak / 重置每日计数
  - 结算"共同登录灰色期"状态机：normal 漏登转 grace；grace 共同登录日照常 +1 并累计恢复日，连续 3 日恢复 normal；grace 漏登 grace_left -1，耗尽清零
  - 通过 date.sync 对齐"共同登录天数"
  - 不一致时以"双方都有活动"的天数计
```

---

## 二、纪念日（date.remind）

### 产品定义

```text
团子记得你们的纪念日，并主动参与：
  - 提前 1 天提醒 + 生成祝福
  - 当天主动弹窗 + 解锁限时装扮 + 邀请互赠
  - 纪念日当天基础互动 ×2、互赠礼物 +20（见上文积分规则）

价值：仪式感自动化的同时，保留"人"的参与
```

### 数据结构

```json
{
  "id": "anniv-001",
  "title": "在一起 100 天",
  "date": "2026-11-08",
  "calendar": "solar",          // solar | lunar
  "repeat": "once",             // once | yearly
  "notify_days_before": 1,
  "gift_hint": "爱心气泡装扮"
}
```

### 触发流程

```text
每日检查（本地，无需联网）：
  ① 读取纪念日列表（农历纪念日先换算为当年公历日期）
  ② 距离提醒日 == notify_days_before
     → LLM 生成祝福语（模板兜底）+ 团子提前预告
     "后天是你们在一起的第 100 天，要我帮你准备个小惊喜吗？"
  ③ 距离提醒日 == 0（当天）
     → 主动弹窗 + 解锁限时装扮 + 邀请互赠（见 05）
     → 自动发 date.remind 给对方 → 双方团子一起庆祝

祝福语生成（离线 LLM）：
  prompt: "写一句给情侣的纪念日祝福，团子口吻，≤20字，卖萌"
  模板兜底: "今天是特别的日子哦！汪汪~ 100 天快乐！"
```

### 日历同步

```text
date.add（添加纪念日）→ 同步对方（双向）
  两端的纪念日列表一致（以"最新 ts 为准"合并）
  单机时：纯本地，不打扰
```

### 隐私

```text
纪念日本身是敏感信息 → 加密同步（走同步层密文）
  仅两端可见，中继不可见
```

---

## 三、与情绪系统联动

```text
共同养成 × 情绪：
  一方持续低落（情绪标签阈值）→ 团子提议"要不要送 TA 一个小惊喜？"
纪念日 × 互赠：
  当天互赠礼物 → 双方解锁专属装扮（见 05）
养成 × 纪念日：
  纪念日当天基础互动积分 ×2、互赠礼物 +20（见积分规则）
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 数据一致性 | [[7-AI学习/11-AI桌宠情侣互联/06-数据模型与离线一致性.md\|数据模型与离线一致性]] |
| 互赠实现 | [[7-AI学习/11-AI桌宠情侣互联/05-互赠与界面交互实现.md\|互赠与界面交互实现]] |
| 情绪系统 | [[7-AI学习/10-AI桌宠实战/07-角色动画与情绪系统.md\|角色动画与情绪系统]] |

---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 总体方案
created: 2026-08-01
---

# 01 总体方案与架构

> 情侣双端互联桌宠的整体方案：定位边界、系统拓扑、设计原则、模块划分与主项目衔接。这是本子项目的纲领性笔记。

---

## 一、定位与边界

```text
✅ 做：情侣间的"连接器 / 催化器"
  - 两个人各养一只团子，团子帮忙传话、传递情绪、一起成长
  - 提供关系中的仪式感与共同记忆
  - 核心价值：让"异地 / 忙碌"的情侣获得存在感与联结感

❌ 不做：
  - 不做"AI 恋人"（替代真人）→ Replika 2023 教训
  - 不做"人设模仿 TA"（虚假替身，伦理雷区）
  - 默认纯本地，互联需用户主动开启
```

### 数据边界

```text
永不上传：
  - 聊天内容、语音录音、本地模型
只同步（密文）：
  - 五类轻量事件：带话 / 情绪标签 / 养成增量 / 纪念日 / 礼物
中继服务器：
  - 只转发密文 + 路由元数据，无法解密
```

### 一句话价值主张

> **"你的团子，替我陪着你。"** —— 团子不是第三者，是你和 TA 之间的信使。

---

## 二、系统架构

### 拓扑

```text
┌─────────────────┐          ┌─────────────────┐
│ A 端桌宠         │          │ B 端桌宠         │
│                 │ WebSocket│                 │
│ UI/动画/TTS      │◄────────►│ UI/动画/TTS      │
│ 状态机           │  E2E 密文 │ 状态机           │
│ 本地 LLM+情绪     │ (中继转发) │ 本地 LLM+情绪     │
│ 记忆/养成         │          │ 记忆/养成         │
│       │          │          │       │          │
│ 同步模块(A)       │          │ 同步模块(B)       │
└───────┬──────────┘          └───────┬──────────┘
        │                             │
        └──────────► 中继(仅转发密文) ◄─┘
                    (可选：局域网直连 / 自托管)
```

### 关键设计原则

```text
1. 离线优先：断网时单机功能完整；互联队列缓冲，恢复后补发
2. 只同步事件，不同步状态：两端各自计算，事件驱动对齐
3. 端到端加密：中继不可信，最小信任模型
4. 节流与防打扰：情绪标签只在显著变化时同步
5. 单向默认关闭：用户不主动开启配对，任何数据不外发
```

---

## 三、模块划分

```text
sync/           同步层（新增，独立线程）
  ├─ pairing.py    配对与密钥交换
  ├─ crypto.py     端到端加密封装（PyNaCl）
  ├─ protocol.py   消息编解码 / 类型定义
  ├─ transport.py  WebSocket 传输（中继 / 局域网直连）
  └─ queue.py      离线队列 + 重试

core/           应用逻辑（扩展）
  ├─ carry.py      带话检测与流程
  ├─ mood_export.py 情绪标签导出（复用 emotion.py）
  ├─ pet_growth.py  亲密度 / 成长计算
  ├─ anniversary.py 纪念日管理与触发
  └─ gift.py        礼物模型与赠送流程

ui/             表现层（扩展）
  ├─ partner_state.py  TA 的状态显示（情绪/在线）
  ├─ gift_dialog.py    互赠界面
  └─ notify.py         收到带话/礼物的弹窗与动画
```

### 模块间通信

```text
新增约束（沿用 04-系统架构设计）：
  1. 同步模块只与 Core 通信，不直接触 UI / 模型
  2. 收到同步事件 → Core 处理 → 信号通知 UI
  3. 网络线程不阻塞推理线程（各自独立）
  4. 同步层异常不影响单机功能（降级为纯离线）
```

---

## 四、与主项目的关系

| 主项目模块 | 复用 | 情侣互联新增 |
|-----------|------|------------|
| 04-系统架构设计 | 进程模型、分层、配置 | sync/ 模块、事件总线扩展 |
| 07-角色动画与情绪系统 | 情绪模型（valence/arousal）、动画状态机 | 情绪标签导出、TA 状态表现 |
| 05-语音交互与感知 | TTS/ASR 管线 | 带话播报（主动 TTS） |
| 08-推理性能与内存优化 | 内存/启动预算 | 同步线程开销（<1% CPU） |
| 09-测试打包与部署 | 打包、更新 | 中继部署、密钥管理 |

### 优先级关系

```text
主线单机 MVP（10-AI桌宠实战）→ 必须先完成
情侣互联 = P2 差异化亮点
  - 架构上预留同步层接口（不阻塞主线）
  - 启动条件：主线 MVP + 周留存 > 30%（内测验证）
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 同步层设计 | [[7-AI学习/11-AI桌宠情侣互联/02-同步层与端到端加密.md\|同步层与端到端加密]] |
| 主项目架构 | [[7-AI学习/10-AI桌宠实战/04-系统架构设计.md\|系统架构设计]] |
| 情绪系统 | [[7-AI学习/10-AI桌宠实战/07-角色动画与情绪系统.md\|角色动画与情绪系统]] |
| 场景调研 | [[7-AI学习/10-AI桌宠实战/13-竞品与应用场景调研.md\|竞品与应用场景调研]] |

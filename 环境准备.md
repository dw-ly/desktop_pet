---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 环境准备
  - Windows
created: 2026-08-03
---

# 环境准备与工具链

> 情侣双端互联桌宠的开发环境搭建指南。**决策：Windows 优先、pip + venv、M2 阶段暂不部署中继（先局域网直连）**。本指南面向"主项目单机桌宠 + 情侣互联同步层"的开发与联调。

---

## 一、总体清单速览

| 类别 | 内容 | 是否必装 |
|------|------|:--------:|
| 基础软件 | Git、VS Code / PyCharm | ✅ |
| 运行时 | Python 3.11（64 位） | ✅ |
| Python 依赖 | AI 栈 + 同步栈 + UI + 测试/打包 | ✅ |
| 模型文件 | LLM / TTS / ASR / Embedding | ✅ |
| 中继服务 | FastAPI 中继（含 Docker） | ⏸ M3 再装 |
| 可选 | CMake + MSVC 工具链（源码编译兜底） | ⚠️ 仅出问题时 |

---

## 二、基础软件

| 软件 | 用途 | 安装方式 |
|------|------|---------|
| Git | 版本管理 | 官网安装包，勾选"Add to PATH" |
| VS Code | 代码编辑 | 官网安装包；建议装 Python、Pylance 扩展 |
| Python 3.11 | 运行环境 | [python.org](https://www.python.org/downloads/) 安装包，**勾选 Add to PATH** |

> 说明：Python 3.11 是当前 PySide6 / llama-cpp-python / sherpa-onnx 兼容性最优的稳定版本。3.12/3.13 也可，但部分轮子可能需要等待预编译版本。

---

## 三、Python 环境（pip + venv）

### 1. 创建项目与虚拟环境

```bash
# 项目代码目录（建议独立于 vault，放在工作目录）
mkdir tuanzi-pet && cd tuanzi-pet

# 创建虚拟环境
python -m venv .venv

# 激活（Windows PowerShell）
.venv\Scripts\Activate.ps1
# 或 CMD
.venv\Scripts\activate.bat
```

### 2. 升级基础工具

```bash
python -m pip install --upgrade pip setuptools wheel
```

### 3. 安装依赖

```bash
# AI 栈（主项目复用）
pip install llama-cpp-python         # LLM 推理（C++ 核心）
pip install sherpa-onnx              # TTS + ASR
pip install chromadb                 # 记忆向量库

# 同步层（情侣互联新增）
pip install pynacl                   # 端到端加密（libsodium）
pip install websockets               # WebSocket 传输
pip install zeroconf                 # mDNS 局域网发现

# 业务模块（情侣互联新增，M3）
pip install lunarcalendar            # 纪念日农历换算（纯 Python，含闰月）

# UI（主项目）
pip install PySide6

# 测试 / 打包
pip install pytest
pip install pyinstaller

# 工具
pip install requests
```

### 4. 固定版本（requirements.txt 参考）

```text
# 建议在首次安装成功后生成锁文件，保证可复现
pip freeze > requirements.txt
```

> 注意：`llama-cpp-python` 在 Windows 上通常有预编译 wheel（`pip install llama-cpp-python` 直接装）。若你的 Python 版本没有对应 wheel 导致源码编译，会需要 CMake + MSVC 工具链（见第八节故障排查）。

---

## 四、模型文件下载

模型统一放在 `models/` 目录（与主项目 04 号架构约定一致）。

### 必下模型

| 模型 | 用途 | 体积 | 来源 |
|------|------|:----:|------|
| Qwen2.5-3B-Instruct GGUF（Q4_K_M） | LLM 对话/带话/祝福 | ~2GB | HuggingFace（Qwen 官方 GGUF） |
| vits-zh-hf-fanchen（sherpa-onnx） | 中文 TTS | ~100-200MB | sherpa-onnx 模型仓库 |
| Paraformer 中文 ASR（sherpa-onnx） | 语音识别 | ~100MB | sherpa-onnx 模型仓库 |
| bge-small-zh-v1.5 | 记忆向量化 | ~95MB | HuggingFace |

### 下载提示

```text
国内网络建议启用 HuggingFace 镜像：
  set HF_ENDPOINT=https://hf-mirror.com

llama-cpp GGUF 下载：HuggingFace 上 Qwen 官方或社区量化版本
sherpa-onnx 模型：GitHub k2-fsa/sherpa-onnx 仓库 releases 页有打包下载
```

### 低配替代

```text
内存 < 8GB 时：用 Qwen2.5-1.5B-Instruct Q4_K_M（~1GB）替代 3B
               参照 10-AI桌宠实战/02-模型选型 的 L0/L1 分级
```

---

## 五、目录结构约定

```text
tuanzi-pet/
├── .venv/                 # 虚拟环境（不入库）
├── models/                # 模型文件
│   ├── qwen2.5-3b-q4.gguf
│   ├── vits-zh/           # TTS
│   ├── paraformer-zh/     # ASR
│   └── bge-small-zh/      # Embedding
├── src/
│   ├── ui/                # 表现层
│   ├── core/              # 应用核心（carry/mood/pet/anniv/gift）
│   └── sync/              # 同步层（pairing/crypto/protocol/transport/queue）
├── tests/                 # 测试
├── config.yaml            # 配置
└── requirements.txt
```

> 运行时数据（身份密钥、数据库）写入用户目录 `~/.tuanzi/`，代码目录保持干净。

---

## 六、联调验证（M2 局域网 demo）

### 1. 单机验证

```bash
python -c "import llama_cpp, sherpa_onnx, nacl, websockets, lunarcalendar, PySide6; print('OK')"
```

无报错即依赖就绪。

### 2. 双端联调流程（同一 WiFi）

```text
1. 两台电脑各自启动桌宠（或同机起两个实例，用不同 data_dir）
2. A 端"添加伴侣" → 生成配对码
3. B 端输入配对码 → 握手成功 → 显示"已连接"
4. A 发一条带话 → B 端收到并播报
5. 拔网线测试离线 → 重连后事件补发
```

> 局域网直连模式不依赖中继，无需任何服务器，本阶段唯一需要的是两台设备在同一网络。

---

## 七、中继服务器（M3 才需要，提前备忘）

```text
待 M3 决定自托管方案后再安装，此处仅记录依赖：
  pip install fastapi uvicorn
  可选 Docker 部署（国内镜像加速配置）

部署形态参考 02-同步层与端到端加密：
  WS /ws/connect · POST /msg/{to} · GET /msg/pending
```

---

## 八、常见问题

| 问题 | 处理 |
|------|------|
| llama-cpp-python 装不上（源码编译报错） | 检查是否缺少 VS Build Tools + CMake；或固定到 Python 3.11 后再装（wheel 最全） |
| HuggingFace 下载慢/失败 | 设 `HF_ENDPOINT=https://hf-mirror.com`；或从国内模型站下载 |
| 防火墙拦截局域网连接 | 允许 Python 通过专用/公用网络；或临时关闭防火墙验证 |
| PySide6 透明窗口在部分显卡异常 | 更新显卡驱动；或用软件渲染 `QT_OPENGL=software` |
| 多实例联调同机冲突 | 两个实例使用不同 data_dir（`--data-dir` 参数）与不同配置端口 |
| 病毒软件误杀 PyInstaller 产物 | 加白名单；测试期直接 `python src/main.py` 运行 |

---

## 九、工程快速迁移（换机一次性搬家）

> 目标：把 tuanzi-pet 工程整体搬到新开发机。最快路径 = **代码走 git、环境靠重建、模型用拷贝、密钥重新配对**。核心原则：只搬运"可再生成本高"的部分（源码、模型、数据），**绝不迁移 `.venv/` 与跨机密钥**（见下方分层表）。

### 1. 迁移分层总览

| 内容 | 处理方式 | 理由 |
|------|---------|------|
| 源码 `src/` `tests/` `config.yaml` `requirements.txt` | git 拉取（或打包拷贝） | 纯文本、体积小、可再生 |
| `.venv/` | **不迁移，新机重建** | venv 记录绝对路径，拷贝后 activate 即损坏 |
| `models/`（约 2.5-3GB） | 移动硬盘/共享目录直接拷贝 | 重下依赖网络且慢，拷贝一次最快 |
| `~/.tuanzi/` 身份密钥 | **不迁移，重新配对** | DPAPI 加密绑定机器，跨机无法解密 |
| `~/.tuanzi/` 联调/开发期运行时数据（DB、记忆向量库，含测试产生的配对/带话/情绪/养成记录） | **默认不迁移**；确需保留时旧机导出备份 → 新机导入 | 测试数据价值在"流程验证"而非数据本身，重跑联调即再生；保留时走 [[7-AI学习/11-AI桌宠情侣互联/06-数据模型与离线一致性.md\|06 换机/重装恢复]] |
| `tests/` 固件与 fixtures | git 拉取（随源码走） | 测试样本随代码版本控制，可复用 |
| pytest 临时产物（临时 DB、测试音频等） | **不迁移** | 应落地临时目录（pytest `tmp_path` 或独立 `data_dir`），测试后自清，不写入 `~/.tuanzi/` |
| 中继配置（M3） | 不迁移 | M2 局域网直连无需服务器 |

> 实践建议：自动化测试统一用独立 `data_dir`（或 pytest `tmp_path`），让测试数据不写入 `~/.tuanzi/`——工程迁移时测试数据天然"零成本"，无需单独处理。

### 2. 新机步骤

```text
1. 装基础软件：Git、VS Code、Python 3.11（勾选 Add to PATH；版本与旧机一致，避免 wheel 兼容问题）
2. 取代码：git clone <私有仓库> tuanzi-pet
   无远程仓库时：zip 打包 src/ tests/ config.yaml requirements.txt 到新机解压
3. 重建环境（一条命令）：.\setup.ps1   ← 见下方脚本
4. 拷模型：旧机 models/ 整个目录拷贝到新机项目根目录（移动硬盘/USB 最快；模型不入 git）
5. 配对与数据：首次启动 → 添加伴侣重新配对；旧机联调/测试数据需保留时，用旧机"导出备份"文件在新机导入（不保留则跳过）
6. 验证：单机跑通 → 双端联调（见第六节流程）
```

### 3. setup.ps1（项目根目录）

```powershell
# setup.ps1 — 新机一键重建环境（换机迁移用）
# 用法：在项目根目录执行  .\setup.ps1

# 1. 检查 Python 版本（建议 3.11）
$py = python --version 2>&1
Write-Host "检测到 $py"
if ($py -notmatch "3\.11") {
  Write-Host "⚠ 建议使用 Python 3.11（wheel 兼容性最优），继续以当前版本尝试…"
}

# 2. 创建并激活虚拟环境
if (-not (Test-Path ".venv")) {
  python -m venv .venv
}
. .venv\Scripts\Activate.ps1

# 3. 升级基础工具并安装依赖（requirements.txt 在旧机用 pip freeze 锁定）
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt

# 4. 校验依赖
python -c "import llama_cpp, sherpa_onnx, nacl, websockets, lunarcalendar, PySide6; print('deps OK')"

# 5. 校验模型目录
if (Test-Path "models") {
  Write-Host "models/ 已就位"
} else {
  Write-Host "⚠ models/ 缺失：请将旧机 models/ 目录拷贝到项目根目录"
}

Write-Host "环境就绪。首次运行请执行『添加伴侣』重新配对。"
```

### 4. 迁移检查表

| 检查项 | 完成 |
|--------|:---:|
| git 远程仓库最新提交已 push | |
| `requirements.txt` 已锁定（旧机 `pip freeze > requirements.txt`） | |
| `models/` 全部文件已拷入新机 | |
| 新机 Python 版本 = 3.11 | |
| `setup.ps1` 运行无报错 | |
| 依赖校验 `deps OK` | |
| 重新配对成功、双端带话通 | |
| 已确认联调/测试数据是否保留（不保留则跳过导出/导入） | |
| `config.yaml` 无残留旧机绝对路径 | |

### 5. 常见坑

| 坑 | 说明 | 处理 |
|----|------|------|
| 直接拷贝 `.venv/` | `pyvenv.cfg` 与脚本路径写死绝对路径，activate 即报错 | 永远重建 venv |
| Python 版本不一致 | 新机装 3.13 时部分轮子（llama-cpp/sherpa-onnx）无预编译 wheel | 统一 3.11 |
| 密钥拷过去用不了 | DPAPI 加密绑定机器 | 重新配对，不迁密钥 |
| 模型没拷全 | 运行时静默缺模型文件 | 对照 `models/` 清单核对 |
| `config.yaml` 绝对路径 | 模型/data 路径仍指旧机 | 改用相对路径或启动时传参 |
| llama-cpp 运行报缺 DLL | 缺 VC++ 运行库 | 安装 Microsoft Visual C++ Redistributable |

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 同步层设计 | [[7-AI学习/11-AI桌宠情侣互联/02-同步层与端到端加密.md\|同步层与端到端加密]] |
| 里程碑 | [[7-AI学习/11-AI桌宠情侣互联/07-里程碑与可行性验证.md\|里程碑与可行性验证]] |
| 主项目环境 | [[7-AI学习/10-AI桌宠实战/09-测试打包与部署.md\|测试打包与部署]] |
| 模型选型 | [[7-AI学习/10-AI桌宠实战/02-模型选型.md\|模型选型]] |

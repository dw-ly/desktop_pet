---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 互赠
  - UI设计
created: 2026-08-01
---

# 05 互赠与界面交互实现

> 情侣互动的"物质载体"：虚拟礼物互赠 + 双端界面交互。礼物是情感的符号化表达，界面是情感传递的舞台。

---

## 一、虚拟礼物系统（gift.*）

### 礼物模型

```json
{
  "giftId": "gift-001",
  "item": {
    "id": "outfit-heart-100",
    "type": "outfit",          // outfit | action | emoji | custom_egg
    "name": "爱心气泡装扮",
    "rarity": "event",          // common | event | anniversary
    "expire_days": 30
  },
  "from": "<pairing-id>",
  "to": "<pairing-id>",
  "ts": 1754040000,
  "state": "sent"               // sent | accepted | expired（退回）
}
```

### 礼物类型

| type | 示例 | 说明 |
|------|------|------|
| outfit | 情侣装扮、纪念日套装 | 解锁后团子外观改变 |
| action | 专属动作（贴贴、摇尾巴） | 团子可演示 |
| emoji | 爱心气泡、表情包 | 聊天/弹窗用 |
| custom_egg | 自定义彩蛋 | 写入双方记忆库，纪念日解锁；纯文本 ≤200 字、每端上限 20 个 |

### 赠送流程

```text
A 选择礼物 → gift.send → B 确认：
  B 弹窗"TA 送了你一份礼物 🎁" → 打开 → gift.accept
  24h 未确认 → 自动 gift.expire → A 端退回（可重新发送）
双方确认 → 各自本地解锁（持久化）+ 亲密度 +5（纪念日当天 +20 特惠，见 04 积分规则）
```

### 解锁持久化

```text
礼物解锁写入本地数据库（见 06 schema）：
  user_items(item_id, name, type, expire_at, source)
  到期自动失效（expire_days）
  端到端一致：gift.accept 同时携带解锁凭证（签名）
```

### 代码骨架

```python
# core/gift.py
def send_gift(gift: dict):
    event = build_event("gift.send", payload=gift)
    sync.send(event)                      # 走同步层加密
    db.insert_local_unlock(gift["item"])  # 乐观解锁（待对方确认）

def on_gift_accept(payload):
    item = payload["item"]
    db.insert_local_unlock(item)          # 正式解锁
    notify.show(f"TA 收下了你的 {item['name']} 🎉")
    pet_growth.add_intimacy(20 if is_anniversary() else 5)
```

---

## 二、双端界面设计

### 主界面新增区域

```text
┌──────────────────────────────┐
│ 团子（表情随 TA 情绪变化）     │
│        ▲                     │
│  [TA 的状态条]  🟢在线 · 团子:小白 · 情绪:开心 │
│  [最近互动]    TA 2h前带了话
│  [快捷操作]  带话 | 送礼物 | 纪念日
└──────────────────────────────┘
```

| 区域 | 说明 |
|------|------|
| 状态条 | TA 在线状态 + 宠物名（pet.profile 同步）+ 最近情绪（只显示标签） |
| 最近互动 | 最近的带话/礼物/纪念日事件（本地记录） |
| 快捷操作 | 一键带话 / 送礼物 / 查看纪念日 |

### 事件弹窗（收到时）

```text
带话：团子开口动画 + "TA 说：……" + TTS 朗读 + [知道了]
礼物：礼物盒动画 + "TA 送了你 🎁" + [打开] [稍后]
纪念日：彩带动画 + "今天是你们的纪念日！" + [庆祝]
情绪变化：无弹窗（静默表现），团子状态条自动更新
```

### 通知免打扰

```text
时段设置：勿扰模式（如 23:00-8:00 不弹窗、不朗读，仅静默收）
  带话/礼物 → 收进"稍后看"列表
  情绪同步 → 始终静默
白名单：勿扰时段内仍可收到（可配置）
```

### 动画与声音

```text
新增动画状态（扩展 07 号状态机）：
  partner_carry   替 TA 说话（张嘴 + 爱心气泡）
  partner_gift    收到礼物（欢呼/跳跃）
  partner_anniv   纪念日（彩带/撒花）
  partner_sleepy  替 TA 表现累（耷拉耳朵）

新增音效：提示音柔和（避免吓到），TTS 用温暖音色
```

---

## 三、交互细节与防误触

```text
带话确认：
  - 发送前必确认（防止误发）
  - 发送后 2 分钟内可撤回（carry.revoke）
礼物：
  - 赠送需二次确认（礼物是"郑重"的动作）
  - 礼物发送后不可撤回（但可过期退回）
纪念日：
  - 只提醒本人 + 对方（不群发）
  - 支持自定义标题（"第一次约会"）
```

---

## 四、可扩展：手机伴侣端（未来）

```text
愿景：PC 桌宠 + 手机 App 伴侣
  - 手机端查看 TA 的团子状态、收带话/礼物
  - 复用同一套协议（加密/事件类型兼容）
  - 技术上：协议已跨平台（libsodium 有移动端绑定）
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 共同养成 | [[7-AI学习/11-AI桌宠情侣互联/04-共同养成与纪念日实现.md\|共同养成与纪念日实现]] |
| 同步层 | [[7-AI学习/11-AI桌宠情侣互联/02-同步层与端到端加密.md\|同步层与端到端加密]] |
| 动画与情绪 | [[7-AI学习/10-AI桌宠实战/07-角色动画与情绪系统.md\|角色动画与情绪系统]] |

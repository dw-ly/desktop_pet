---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 端到端加密
  - 同步层
created: 2026-08-01
---

# 02 同步层与端到端加密

> 情侣互联的基础设施：配对与密钥交换、消息协议、中继部署与局域网直连。所有上层功能（带话/情绪/养成/纪念日/互赠）都复用这一层。设计目标：**中继不可信，数据只能两端解密**。

---

## 一、密码学选型

| 项目 | 方案 | 说明 |
|------|------|------|
| 密钥交换 | **X25519**（Curve25519 ECDH） | 现代标准，性能好，libsodium 内置 |
| 对称加密 | **XSalsa20-Poly1305**（secretbox） | 认证加密，防篡改 + 防伪造 |
| 随机数 | libsodium `randombytes` | 系统 CSPRNG |
| Python 库 | **PyNaCl** | libsodium 官方绑定，跨端协议兼容 |

```text
为什么选 libsodium 而不是自研/openssl：
  1. 高级 API（secretbox 一条 API 完成加密+认证）
  2. 默认安全参数，避免误用
  3. 协议与移动端/其他语言可互通（未来可做手机伴侣端）
```

---

## 二、配对与密钥管理

### 配对流程

```text
① 生成本地身份密钥对（X25519，持久化到 ~/.tuanzi/identity.key）
② 配对：
   A 点击"添加伴侣" → 生成配对码（含 A 公钥 + 一次性随机 nonce）
   显示为：12 位短码（如 TUANZI-3F8K2M）
   配对码参数：10 分钟有效 · 输错 5 次作废 · 配对成功后一次性消费
   B 输入配对码（或扫码）→ 完成交换
③ 握手：
   双方各自用对方公钥 + 自己私钥做 X25519 → 得到共享秘密
   再派生双向会话密钥（HKDF，分离 A→B 与 B→A）
④ 验证：
   交换一次性验证码（短认证标签），防止中间人
   完成 → 双方显示"已连接"
```

### 密钥存储

```text
identity.key   本地私钥（权限 600）
peers/<id>.key 已配对伴侣的共享会话密钥
重配对：换设备 / 重置时重新走配对码流程
吊销：任一方向对方发送 pairing.revoke，清除本地配对
```

### 代码示例

```python
# crypto.py —— 基于 PyNaCl 的封装
from nacl.public import PrivateKey, PublicKey, Box
from nacl.encoding import HexEncoder
from nacl.utils import random
import base64, hashlib

def gen_identity() -> bytes:
    return bytes(PrivateKey.generate())

def pairing_code(pub_key_hex: bytes, nonce: bytes) -> str:
    """生成 12 位配对码：前 6 位校验 + 后 6 位随机"""
    h = hashlib.blake2b(pub_key_hex + nonce, digest_size=3).digest()
    raw = (h + nonce[:3])
    return "TUANZI-" + base64.b32encode(raw).decode()[:11].replace("=", "")

def derive_session(my_priv: bytes, peer_pub: bytes) -> Box:
    """握手后得到双向会话加密对象"""
    return Box(PrivateKey(my_priv), PublicKey(peer_pub))

def seal(box: Box, plaintext: bytes) -> bytes:
    return box.encrypt(plaintext)   # 自动处理 nonce + MAC

def open(box: Box, ciphertext: bytes) -> bytes:
    return box.decrypt(ciphertext)  # 验证失败抛异常
```

---

## 三、消息协议

### 信封结构（密文内部）

```json
{
  "v": 1,
  "type": "msg.carry",
  "from": "<pairing-id>",
  "seq": 42,
  "ts": 1754040000,
  "payload": { "...": "..." }
}
```

| 字段 | 说明 |
|------|------|
| v | 协议版本 |
| type | 事件类型（下表） |
| from | 发送方 pairing-id |
| seq | 发送端单调递增序号（去重用） |
| ts | 发送端时间戳（排序用） |
| payload | 各类型自定义负载 |

### 事件类型表

| type | 方向 | payload 要点 | 说明 |
|------|:----:|-------------|------|
| `hello` / `hello.ack` | 双向 | 设备信息、在线状态 | 连接握手 |
| `pairing.revoke` | 双向 | 无 | 解除配对（吊销密钥） |
| `msg.carry` | A→B | text、carryId、expireAt | 带话 |
| `carry.ack` | B→A | carryId、readAt | 送达确认 |
| `carry.revoke` | A→B | carryId | 带话撤回（2 分钟内） |
| `mood.sync` | 双向 | valence、arousal、label | 情绪标签 |
| `pet.feed` | 双向 | delta、reason、ts、sig | 养成增量（亲密度积分，带 HMAC 签名） |
| `pet.profile` | 双向 | petName、ts | 宠物名同步（改名重发） |
| `date.add` / `date.remind` | 双向 | 日期、标题、祝福语 | 纪念日新增/提醒 |
| `date.sync` | 双向 | streak、intimacy、ts | 每日零点对齐（累计数据快照） |
| `gift.send` / `gift.accept` / `gift.expire` | 双向 | giftId、item、to | 互赠 |

### 传输层信封（中继可见部分）

```json
{
  "to": "<peer-pairing-id>",
  "cipher": "<base64 密文>",
  "expires": 1754043600
}
```

> 中继只看到：发给谁、密文、过期时间。**无法看到 type / from / payload**。

---

## 四、中继服务器（可选）

### 设计

```text
职责：身份路由 + 密文转发 + 离线暂存（TTL，默认 7 天）
  - 不存聊天记录、不存会话密钥、不存明文
实现：FastAPI + websockets，无状态（可水平扩展）
部署：① 公共中继（方便）② 自托管（最隐私）③ 局域网直连（零服务器）
```

### 传输流程

```text
A 发送：
  A 同步模块 → (加密) → 中继 receive → 查 B 在线状态
    ├─ B 在线 → 立即转发
    └─ B 离线 → 暂存（TTL）→ B 上线时补发
B 接收：
  B 同步模块 → 解密 → 校验 seq（去重）→ 投递到 Core
```

### 局域网直连（零服务器模式）

```text
场景：同一 WiFi / 同一网络
发现：mDNS（zeroconf）广播本机 pairing-id
连接：WebSocket 直连 IP:PORT
安全：仍走 E2E 加密（同一套协议）
退出：切换到中继模式（异地时）
```

### 中继 API（参考）

| 方法 | 路径 | 说明 |
|------|------|------|
| WS | `/ws/connect` | 长连接（身份认证 = 配对公钥签名） |
| POST | `/msg/{to}` | 投递密文（HTTP 兜底） |
| GET | `/msg/pending` | 拉取离线暂存 |

---

## 五、连接状态机

```text
            ┌────────┐
  配对成功   │ paired │
            └───┬────┘
      启动     │
              ▼
     ┌──────────────┐   网络恢复   ┌────────────┐
     │ disconnected │◄────────────│ connecting │
     └──────┬───────┘             └─────┬──────┘
            │ 连接建立                   │ 失败重试
            ▼                            │
     ┌────────────┐  心跳超时   ┌─────────┴───────┐
     │ connected  │───────────►│ reconnecting    │
     └────────────┘            └─────────────────┘

心跳：每 30s 发 hello，3 次无响应 → 判定离线
离线行为：事件入本地队列（queue.py），恢复后按 seq 补发
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 总体方案 | [[7-AI学习/11-AI桌宠情侣互联/01-总体方案与架构.md\|总体方案与架构]] |
| 数据一致性 | [[7-AI学习/11-AI桌宠情侣互联/06-数据模型与离线一致性.md\|数据模型与离线一致性]] |
| 主项目架构 | [[7-AI学习/10-AI桌宠实战/04-系统架构设计.md\|系统架构设计]] |

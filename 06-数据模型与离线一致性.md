---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 数据模型
  - 一致性
created: 2026-08-01
---

# 06 数据模型与离线一致性

> 双端数据的持久化与一致性设计。难点不在于"存什么"，而在于"两端如何收敛到一致"。原则：**轻游戏化标准，事件驱动 + 时间戳合并，不追求银行级一致性**。

---

## 一、本地数据库 Schema（SQLite）

```sql
-- 配对与伴侣信息（落地 schema 见 spec/data-consistency/impl §2.2；会话密钥归 sync key_store，不冗余存储）
CREATE TABLE pairing (
  id INTEGER PRIMARY KEY,
  pairing_id TEXT UNIQUE,        -- 本机身份
  peer_id TEXT,                  -- 伴侣 pairing-id
  peer_name TEXT,                -- 伴侣用户昵称
  peer_pet_name TEXT,            -- 伴侣宠物名（pet.profile 同步）
  created_at INTEGER
);

-- 事件日志（去重 + 审计 + 发送队列；行号主键 id，避免与发送序号 seq 混淆）
-- 落地 schema 见 spec/data-consistency/impl §2.2 + §4.1（0002 迁移收编 sync_queue 的 outbox/seen/meta）：
--   status：发送侧 pending/sent/failed；接收侧 received（默认）
--   seq：发送方序号（离线补发按 seq 升序）
--   expires_at：发送侧过期时间（超期补发标记 failed）
CREATE TABLE events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,   -- 本端行号
  event_id TEXT UNIQUE,                    -- 全局唯一（from:seq，去重键）
  type TEXT,                               -- msg.carry / mood.sync ...
  peer TEXT,
  payload_json TEXT,                       -- 明文（仅本端可见）
  created_at INTEGER,
  acked_at INTEGER,                        -- 送达确认时间
  status TEXT NOT NULL DEFAULT 'received',
  expires_at INTEGER,
  seq INTEGER
);
CREATE INDEX idx_events_queue ON events(status, peer, seq);

-- 持久化 kv（收编 sync_queue 的 meta 表：seq 游标等；0002 迁移）
CREATE TABLE kv (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

-- 带话（落地 schema 见 spec/carry-message/impl §3.1；撤回/确认竞态收敛见 §4.4）
CREATE TABLE carries (
  id TEXT PRIMARY KEY,          -- carryId
  direction TEXT,               -- out / in
  text TEXT,
  status TEXT,                  -- draft/sent/delivered/revoked/failed
  expires_at INTEGER,
  created_at INTEGER,
  sent_at INTEGER,              -- 发送方发送时间（撤回窗口基准）
  read_at INTEGER,              -- 送达确认时间（ack）
  revoked_at INTEGER            -- 撤回时间
);

-- 礼物（发送记录）
CREATE TABLE gift_offers (
  gift_id TEXT PRIMARY KEY,     -- giftId
  from_peer TEXT,               -- 发送方 pairing-id
  to_peer TEXT,                 -- 接收方 pairing-id
  item_id TEXT,                 -- 对应 user_items.item_id
  state TEXT,                   -- sent / accepted / expired
  sent_at INTEGER,
  expire_at INTEGER,            -- 24h 过期判定
  accepted_at INTEGER
);

-- 礼物（解锁物品）
CREATE TABLE user_items (
  item_id TEXT PRIMARY KEY,
  name TEXT, type TEXT, rarity TEXT,
  source TEXT,                  -- 谁送的 / 解锁来源
  expire_at INTEGER
);

-- 养成状态（本端一份）
CREATE TABLE pet_state (
  key TEXT PRIMARY KEY,         -- level/exp/intimacy/streak_days
  value INTEGER
);

-- 纪念日
CREATE TABLE anniversaries (
  id TEXT PRIMARY KEY,
  title TEXT, date TEXT, repeat TEXT,
  calendar TEXT,                -- solar / lunar
  notify_days_before INTEGER,
  updated_at INTEGER
);
```

### 密钥存储安全

```text
session_key 用本机密钥环加密后再存（Windows DPAPI / Linux SecretService）
  - 避免明文落盘
  - 简单兜底：独立文件权限 600 + 提示（个人项目可接受）
```

### 版本化与迁移（自研轻量版本表）

```text
schema_version 表记录当前库版本（初始 1）
migrations/NNN_*.sql 为有序增量脚本，启动时执行编号 > 当前版本 的脚本
  每个脚本在事务内执行 → 成功后 UPDATE version → 失败回滚并提示用户
约定：新增表/字段一律以迁移脚本追加，不直接修改已发布的表结构
```

```sql
CREATE TABLE schema_version (version INTEGER NOT NULL);
INSERT INTO schema_version VALUES (1);
```

---

## 二、事件去重

```text
问题：网络重试 / 双发可能造成重复投递
方案：event_id = "{from}:{seq}" 全局唯一
  - 收到事件先查 events.event_id
  - 已存在 → 丢弃（幂等）
  - 不存在 → 插入 + 投递 Core
```

```python
# sync/queue.py —— 去重（收编后 events 表承载；单事务"插入+投递"原子）
def receive(msg, deliver) -> bool:
    with db.transaction():
        cur = db.execute(
            "INSERT OR IGNORE INTO events(event_id, seq, type, peer, payload_json, "
            "created_at, status) VALUES(?, ?, ?, ?, ?, ?, 'received')",
            (f"{msg.from_id}:{msg.seq}", msg.seq, msg.type, msg.from_id, json.dumps(msg.payload), now))
        if cur.rowcount == 0:
            return False            # 重复，丢弃
        deliver(msg)                # 投递抛异常 → 整体回滚
        return True
```

---

## 三、离线队列与重试

```text
发送侧（收编后入 events 表，status='pending'）：
  网络不可用 → 事件入队（event_id = "{本端}:{seq}"）
  重连后 → 按 seq 升序补发（events 表 WHERE status='pending' ORDER BY seq）
  带过期时间的事件（expires_at）→ 超期补发标记 failed
  meta（seq 游标）收编到 kv 表，序号不因迁移回退

接收侧：
  中继离线暂存（TTL 7 天）→ 上线后拉取
  本地不保存未接收事件（中继负责暂存）
```

```python
async def flush_outbox(peer_id, send_cb):
    for ev in db.query_all(
        "SELECT * FROM events WHERE peer=? AND status='pending' ORDER BY seq", (peer_id,)):
        if ev["expires_at"] and now > ev["expires_at"]:
            db.execute("UPDATE events SET status='failed' WHERE id=?", (ev["id"],))
            continue
        try:
            await send_cb(Message(...ev["seq"]...))
            db.execute("UPDATE events SET status='sent' WHERE id=?", (ev["id"],))
        except NetworkError:
            continue            # 网络未恢复，保持 pending
```

---

## 四、事件日志清理

```text
events 表只增不删 → 长期膨胀，需要清理。规则：
  可删（满足其一，且超过保留期 90 天）：
    - 接收侧记录：插入即已投递处理，created_at 超期即可删
    - 发送侧记录：acked_at 非空（已收到送达确认）
    - 对应业务已判失败：带话 status=failed / 礼物 state=expired
  保留：仍在去重/补发窗口内（未确认未过期，或 90 天内）
  边界：只动 events 表；carries / gift_offers / user_items / pet_state / anniversaries 不参与
触发：启动时 + 每日零点各一次
分批：每批 1000 行，避免长事务锁库
```

```python
def prune_event_log(retention_days=90):
    """启动时 + 每日零点调用；分批删除防长事务"""
    cutoff = int(time.time()) - retention_days * 86400
    while True:
        n = db.execute(
            "DELETE FROM events WHERE created_at < ? AND acked_at IS NOT NULL LIMIT 1000",
            (cutoff,)).rowcount
        db.commit()
        if n == 0:
            break
    # 业务已判失败的事件（带话/礼物过期）由各业务模块在状态变更时
    # 同步标记对应 events 行，纳入下一轮清理
```

---

## 五、冲突合并策略

### 通用规则：最新时间戳为准（LWW）

```text
两端各存一份状态，事件带 ts：
  - 同一键更新 → 取 ts 大者
  - 不同键 → 各自保留（无冲突）
场景：
  亲密度积分 → 事件流，两端按 ts 重放收敛
  装扮解锁 → 以 gift.accept 事件为准（一次性）
  纪念日列表 → date.add 事件，同 id 取最新 ts
```

### 亲密度的一致性

```text
策略：不直接同步"亲密度数字"，同步"积分事件"
  - 每次 +3/+5/+10/+20 是一个事件（带 sig）
  - 两端各自从"双方事件流"重放 → 得到一致总和
  - 兜底：每日 0 点双向 date.sync 全量核对，差异以日志修复

防作弊：
  - 每个增量事件带 HMAC 签名（会话密钥派生）
  - 每日各 reason 有上限（见 04 积分规则）
```

### 乐观锁示例

```python
# 亲密度合入（事件重放）
def apply_intimacy_event(ev):
    if not verify_sig(ev.sig, ev.delta):       # 签名校验
        return False
    if daily_limit_exceeded(ev.reason):        # 每日上限
        return False
    pet_state["intimacy"] += ev.delta
    return True
```

---

## 六、换机 / 重装恢复

```text
场景：一端换电脑，本地数据丢失
方案：
  ① 重配对：重新走配对码（新密钥对）
  ② 数据恢复源（二选一）：
     a) 对方端"导出备份" → 本地导入（含养成/装扮/纪念日）
     b) 中继密文备份（可选）：只存加密状态快照，本人密码可解
  ③ 恢复后：以"双方事件流"再次重放对齐

隐私：
  备份必须是密文（用户口令派生密钥加密，scrypt 派生）
  中继不可解；口令由用户自行保管，遗忘则无法恢复
  （口令派生独立于配对状态：换机重新配对后旧备份仍可解密）
```

---

## 七、同步策略总览

| 数据类型 | 同步方式 | 一致性 | 冲突处理 |
|---------|---------|--------|---------|
| 带话/礼物 | 事件（即时） | 最终一致 | 幂等去重 + 过期 |
| 情绪标签 | 事件（节流） | 弱一致 | 丢弃旧值（只认最新） |
| 亲密度 | 增量事件 | 最终一致 | LWW + 签名 + 重放 |
| 装扮解锁 | 事件（一次性） | 强一致（确认制） | accept 为准 |
| 纪念日 | 事件（双向） | 最终一致 | 同 id LWW |

```text
可接受的降级：
  短时不一致（秒级）可接受
  长期不一致 → 每日 date.sync 兜底对齐
  重大异常（签名失败/重放溢出）→ 停止合入并提示用户手动同步
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 同步层 | [[7-AI学习/11-AI桌宠情侣互联/02-同步层与端到端加密.md\|同步层与端到端加密]] |
| 共同养成 | [[7-AI学习/11-AI桌宠情侣互联/04-共同养成与纪念日实现.md\|共同养成与纪念日实现]] |
| 记忆系统 | [[7-AI学习/10-AI桌宠实战/04-系统架构设计.md\|系统架构设计（记忆模块）]] |

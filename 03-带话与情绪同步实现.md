---
tags:
  - AI
  - 桌宠
  - 情侣场景
  - 带话
  - 情绪同步
created: 2026-08-01
---

# 03 带话与情绪同步实现

> 情侣互联的两大核心"存在感"功能：**带话**（替你开口）与**情绪同步**（TA 的状态可见）。两者都是低频、轻量、高情感价值的事件。

---

## 一、带话（msg.carry）

### 产品定义

```text
用户对团子说一句话，团子把它带给对方
  - "告诉 TA 今晚早点睡"
  - "跟 TA 说我想 TA 了"
  → 对方桌宠主动播报（TTS + 弹窗 + 开口动画）

价值：降低"主动开口"的心理门槛，提供撒娇/传达情绪的媒介
```

### 意图检测（M2 纯规则 + M3 分类模型增强）

```text
规则命中（低成本，覆盖 80%，M2 首版）：
  "告诉 / 帮我告诉 / 转告 / 跟他说 / 跟她说 / 对TA说 / 帮我传话"
未命中 → M2 按普通对话处理（不进入带话流程）
  M3 内测后增强：轻量分类模型二分类（carry / not-carry）
  复用 02-模型选型的 sub-1B 分类模型思路，利用内测语料训练
```

```python
# core/carry.py
RULES = ["告诉", "帮我告诉", "转告", "跟他说", "跟她说", "对 ta 说", "帮我传话"]

def detect_carry(text: str) -> bool:
    """M2 首版：纯规则匹配；未命中按普通对话处理"""
    t = text.lower()
    return any(k in t for k in RULES)

def extract_target_and_msg(text: str) -> tuple[str, str]:
    """抽取目标（TA）与正文 → 生成正式带话文本"""
    # 简化：去掉"告诉/转告/跟TA说"前缀，剩余为正文
    for k in RULES:
        if k in text.lower():
            return "partner", text.replace(k, "", 1).strip()
    return "partner", text
```

### 完整流程

```text
A 端：
  ① 用户对团子说带话（文字/语音）
  ② detect_carry → 命中 → 确认弹窗："要把这句话带给 TA 吗？"
  ③ 生成正式文本（M2 默认原话；LLM 润色 M3 内测后按需评估）
  ④ msg.carry 加密 → 发送 → 本地记录状态 pending

中继：转发密文（B 在线即时 / 离线暂存）

B 端：
  ⑤ 解密 → 去重 → 投递 Core
  ⑥ 团子主动播报：弹窗 + TTS 朗读 + "开口"动画
  ⑦ 用户点击"知道了" → 回 carry.ack
  B 未响应 → 3 分钟后自动 ack（延后至 2 分钟撤回窗口之外，保证撤回窗口完整有效）

A 端收 ack：
  ⑧ 显示"TA 的团子已转达 ✓"（带话状态机 pending → delivered）
```

### 带话状态机

```text
      ┌─────────┐ 确认发送  ┌─────────┐ 收到 ack  ┌──────────┐
      │  draft  │─────────► │  sent   │─────────► │ delivered│
      └─────────┘           └────┬────┘          └──────────┘
                      2 分钟内撤回│      │ 超时未送达（24h）
                                ▼      ▼
                           ┌────────┐ ┌────────┐
                           │ revoked│ │ failed │
                           └────────┘ └────────┘
```

### UI 表现

```text
A 端：气泡显示"已交给团子"→ 转达后更新为"已送达"
B 端：团子张嘴说话动画 + 弹窗"TA 说：……" + TTS 朗读
  表情：按文本情绪给 happy / miss / caring 对应动画
```

---

## 二、情绪同步（mood.sync）

### 产品定义

```text
团子知道主人的状态 → 在对方桌面表现出相应情绪
  - 你在加班，团子耷拉耳朵 → TA 那边看到"TA 今天很累"
  - 你心情好，团子摇尾巴 → TA 那边是开心的小柴

价值：不需要对方主动汇报，"看团子就知道 TA 好不好"
```

### 情绪导出（复用 07-角色动画与情绪系统）

```text
本地情绪模型（07 号：valence + arousal）周期汇总：
  {valence: -0.6, arousal: 0.2, label: "sleepy"}

label 映射（与 07 号 EMOTION_MAP 对齐，共 8 个）：
  happy / excited / calm / neutral / sad / angry / confused / sleepy
```

### 节流与防打扰

```text
同步条件（同时满足才发）：
  1. 情绪显著变化：|Δvalence| ≥ 0.3 或 |Δarousal| ≥ 0.3
  2. 距上次同步 > 10 分钟
  3. 用户未开启"隐身模式"
```

```python
# core/mood_export.py
LAST_SYNC_TS = 0
LAST_SENT = None

def maybe_sync_mood(emotion) -> bool:
    global LAST_SYNC_TS, LAST_SENT
    now = time.time()
    if now - LAST_SYNC_TS < 600:
        return False                      # 节流
    if LAST_SENT and abs(emotion.valence - LAST_SENT["valence"]) < 0.3 \
                  and abs(emotion.arousal - LAST_SENT["arousal"]) < 0.3:
        return False                      # 无显著变化
    LAST_SYNC_TS = now
    LAST_SENT = {"valence": emotion.valence, "arousal": emotion.arousal,
                 "label": emotion.label}
    send_mood_sync(LAST_SENT)             # 走 sync 层加密发送
    return True
```

> 情绪标签不持久化，仅保留当前值、新值覆盖旧值（与 06 号同步策略一致），不建表。

### 接收端表现映射

| 收到 label | B 端团子表现 |
|-----------|------------|
| sleepy | 耷拉耳朵、趴着、无精打采 |
| sad | 低头、尾巴垂下 |
| happy / excited | 摇尾巴、跳跃 |
| angry | 耳朵后贴、警惕姿态 |
| confused | 歪头、困惑表情、左右张望 |
| neutral / calm | 正常待机 |

```text
实现：partner_state.py 维护"TA 的情绪"→ 覆盖本端团子的空闲态
     （仅在无本地交互时生效，本地交互优先）
```

### 隐私保证

```text
只同步情绪标签（valence/arousal/label），绝不同步聊天内容
TA 看不到任何对话原文，只看团子的"状态"
可随时关闭：设置 → 隐私 → "隐身模式"（停止发送情绪）
```

---

## 三、与其他功能的联动

```text
带话 + 情绪：带话文本可附带当前情绪 → 对方团子用对应语气朗读
情绪 + 养成：长期低落 → 团子提醒"要不要给对方一个小惊喜？"（促互赠）
情绪 + 纪念日：纪念日前心情差 → 主动提议"帮 TA 准备个惊喜吧"
```

---

## 相关笔记

| 主题 | 笔记 |
|------|------|
| 返回子项目索引 | [[7-AI学习/11-AI桌宠情侣互联/AI桌宠情侣互联 笔记索引.md\|AI桌宠情侣互联 索引]] |
| 同步层 | [[7-AI学习/11-AI桌宠情侣互联/02-同步层与端到端加密.md\|同步层与端到端加密]] |
| 情绪系统 | [[7-AI学习/10-AI桌宠实战/07-角色动画与情绪系统.md\|角色动画与情绪系统]] |
| 语音交互 | [[7-AI学习/10-AI桌宠实战/05-语音交互与感知.md\|语音交互与感知]] |
